# Benchmark Python deps (MMORE installed separately in install.sh).
datasets>=2.14.0
scipy>=1.11.0
openai>=1.0.0        # GT annotation; optional OPENAI_BASE_URL for vLLM
huggingface_hub>=0.20.0
httpx>=0.27.0
