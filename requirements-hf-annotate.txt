# Optional for HF local ground-truth (e.g. meta-llama/Llama-3.1-8B-Instruct).
# Install: INSTALL_HF_ANNOTATE=1 bash install.sh
# Or later: pip install -r requirements-hf-annotate.txt
torch>=2.1.0
transformers>=4.43.0
accelerate>=0.33.0
sentencepiece>=0.2.0
