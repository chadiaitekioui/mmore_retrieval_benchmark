# Optional — ground truth with Hugging Face (e.g. Llama-3.1-8B-Instruct).
# Install: pip install -r requirements.txt -r requirements-hf-annotate.txt
-r requirements.txt
torch>=2.1.0
transformers>=4.43.0
accelerate>=0.33.0
sentencepiece>=0.2.0
